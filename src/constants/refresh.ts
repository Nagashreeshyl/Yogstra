/** Background refresh interval used across the app (with Supabase realtime). */
export const APP_REFRESH_INTERVAL_MS = 15_000

/** Fast poll while waiting for an incoming/outgoing DM video call (mobile + realtime fallback). */
export const INCOMING_CALL_POLL_MS = 2_000
