/** Space reserved above the floating mobile tab bar (bar + margin + safe area). */
export const MOBILE_TAB_BAR_CLEARANCE =
  'calc(3.75rem + 1.25rem + max(0.75rem, env(safe-area-inset-bottom)) + 1rem)'

/** Bottom inset for full-screen mobile overlays (chat, modals) above the tab bar. */
export const MOBILE_TAB_BAR_BOTTOM = MOBILE_TAB_BAR_CLEARANCE
